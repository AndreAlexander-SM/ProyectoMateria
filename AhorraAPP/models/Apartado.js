export class Apartado {
    constructor(id, nombre, monto, mes, categoria, descripcion) {
        this.id = id;
        this.nombre = nombre;
        this.monto = monto;
        this.mes = mes;
        this.categoria = categoria;
        this.descripcion = descripcion;
    }
}